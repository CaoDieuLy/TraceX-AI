# TraceX-AI Environment — LightningAI Deployment
# Set these in Coolify Environment Variables panel

# Database
POSTGRES_DB=video_tracking
POSTGRES_USER=mcpt_user
POSTGRES_PASSWORD=change-me-in-production

# Security
JWT_SECRET_KEY=change-me-use-openssl-rand-base64-64

# Paths (on LightningAI)
A20_ROOT=/workspace/a20-root
HF_HOME=/workspace/models/huggingface
TORCH_HOME=/workspace/models/torch

# Model paths
SEAMLESS_M4T_MODEL=facebook/seamless-m4t-v2-large

# Storage
STORAGE_BASE_URL=https://tracex-ai.smartnovi.tech/storage

# Bootstrap admin
BOOTSTRAP_ADMIN_EMAIL=admin@mcpt.local
BOOTSTRAP_ADMIN_PASSWORD=Admin@123456
BOOTSTRAP_ADMIN_FULL_NAME=Administrator

# GPU settings
TRANSFORMERS_OFFLINE=0

# Trace clip rendering
# Keep these conservative so trace video generation does not starve ingest,
# embedding, or query ranking.
TRACE_CLIP_PARALLEL_WORKERS=2
TRACE_CLIP_FFMPEG_THREADS=2
TRACE_CLIP_VALIDATE_TIMEOUT_SECONDS=0
