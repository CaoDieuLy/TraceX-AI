# Trình duyệt cần trỏ tới cổng backend đã map ra máy host.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
# Next.js server (trong container frontend) gọi backend qua DNS nội bộ Docker.
INTERNAL_API_GATEWAY_URL=http://backend:8000
